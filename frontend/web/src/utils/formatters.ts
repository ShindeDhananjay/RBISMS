// Utility formatters
// formatCurrency(value): ₹1,23,456.00 (Indian number format)
// formatNumber(value): 1,23,456 (Indian lakh/crore format)
// formatDate(date): DD/MM/YYYY
// formatDateTime(date): DD/MM/YYYY HH:mm
// formatPercent(value): 87.5%
// formatMobile(mobile): +91-XXXXX-XXXXX
// truncateText(text, maxLen): truncates with ellipsis
