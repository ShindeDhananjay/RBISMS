// General helper utilities
// generateGoogleMapLink(lat, lng): returns Google Maps URL
// calculateBusinessScore(villageData): weighted scoring algorithm
// getInitials(name): "Rajesh Kumar" → "RK"
// deepClone(obj), groupBy(array, key), sortByKey(array, key)
// buildQueryString(params): { page: 1, size: 10 } → "?page=1&size=10"
