// Main application layout
// Structure: Sidebar (collapsible) + TopHeader + Content area + Footer
// Sidebar shows navigation items filtered by user role
// TopHeader shows: breadcrumb, notification bell (badge), user avatar dropdown
